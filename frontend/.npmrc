# react-joyride@2.9.3 declares a peer range of react "15 - 18", but works fine
# on react 19 (which is what the tour ships on). Without this, `npm ci` in the
# Docker build fails with ERESOLVE even though the lockfile is valid.
legacy-peer-deps=true
